# front-end
DEV:
 1. Adjust `ENVIRONMENT` variable in .env under client/ and server/
 2. Use `PORT=3000 nodemon start`

PROD:
 1. Adjust `ENVIRONMENT` variable in .env under client/ and server/
 2. Use `nodemon start`
